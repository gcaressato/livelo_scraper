name: Livelo Partners Scraper
on:
  schedule:
    - cron: '0 12 * * *'  # Executa todos os dias às 12:00 UTC
  workflow_dispatch:  # Permite execução manual

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 45  # Timeout de 45 minutos
    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4
        
      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          
      - name: Configurar Chrome
        uses: browser-actions/setup-chrome@latest
        
      - name: Instalar dependências
        run: |
          pip install selenium webdriver-manager pandas openpyxl selenium-stealth plotly numpy requests
          python -c "import selenium; print(f'Selenium version: {selenium.__version__}')"
          google-chrome --version
          
      - name: Criar diretórios necessários
        run: |
          mkdir -p logs
          mkdir -p relatorios
          
      - name: Executar pipeline principal
        run: |
          echo "🚀 Iniciando pipeline Livelo Analytics..."
          python main.py 2>&1 | tee pipeline.log
          echo "PIPELINE_STATUS=$?" >> $GITHUB_ENV
        continue-on-error: true
          
      - name: Verificar resultados do pipeline
        run: |
          echo "📊 Verificando resultados do pipeline..."
          
          if [ "$PIPELINE_STATUS" = "0" ]; then
            echo "✅ Pipeline executado com sucesso"
            echo "PIPELINE_OK=true" >> $GITHUB_ENV
          else
            echo "❌ Pipeline com problemas"
            echo "PIPELINE_OK=false" >> $GITHUB_ENV
            # FALHA EXPLÍCITA - main.py já fez todas as validações
            exit 1
          fi
          
          # Verificar arquivos críticos
          if [ -f "relatorio_livelo.html" ] && [ -f "livelo_parceiros.xlsx" ]; then
            echo "✅ Arquivos principais encontrados"
            ls -la *.html *.xlsx
            echo "FILES_OK=true" >> $GITHUB_ENV
          else
            echo "❌ Arquivos principais ausentes"
            echo "FILES_OK=false" >> $GITHUB_ENV
            exit 1
          fi
          
          # Verificar tamanho do HTML (verificação rápida adicional)
          if [ -f "relatorio_livelo.html" ]; then
            size=$(stat -f%z "relatorio_livelo.html" 2>/dev/null || stat -c%s "relatorio_livelo.html" 2>/dev/null || echo "0")
            if [ "$size" -gt 50000 ]; then
              echo "✅ HTML válido: $size bytes"
            else
              echo "❌ HTML muito pequeno: $size bytes"
              echo "FILES_OK=false" >> $GITHUB_ENV
              exit 1
            fi
          fi
        
      - name: Preparar arquivos para GitHub Pages
        run: |
          echo "📁 Preparando arquivos para GitHub Pages..."
          
          # Criar estrutura para GitHub Pages
          mkdir -p _site
          
          # Copiar arquivos para _site (estrutura do GitHub Pages)
          cp relatorio_livelo.html _site/index.html
          cp livelo_parceiros.xlsx _site/
          
          # Criar também a pasta relatorios se necessário
          mkdir -p _site/relatorios
          cp relatorio_livelo.html _site/relatorios/livelo_analytics.html
          
          echo "✅ Arquivos preparados para GitHub Pages"
          echo "📂 Estrutura do _site:"
          ls -la _site/
          ls -la _site/relatorios/ 2>/dev/null || echo "Pasta relatorios criada"
          
      # Upload dos arquivos como artifact
      - name: Upload páginas como artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site
          
      - name: Configurar Git para commit
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Actions"
          
      - name: Commit e push dos arquivos atualizados
        run: |
          echo "💾 Fazendo commit dos arquivos atualizados..."
          
          timestamp=$(date +"%d/%m/%Y %H:%M")
          
          # Adicionar arquivos (manter estrutura original também)
          git add -f relatorio_livelo.html
          git add -f livelo_parceiros.xlsx
          git add -f *.log 2>/dev/null || true
          
          # Verificar se há mudanças para comitar
          if git diff --staged --quiet; then
            echo "ℹ️ Nenhuma mudança detectada para commit"
          else
            echo "📝 Mudanças detectadas, fazendo commit..."
            
            git commit -m "📊 Atualização Livelo Analytics - $timestamp

            ✅ Pipeline: Sucesso com validação rigorosa
            📄 Arquivos: Validados (min 50 parceiros)
            📁 Preparado para GitHub Pages
            ⏰ Executado: $timestamp"
            
            # Push com retry
            echo "🚀 Fazendo push das mudanças..."
            for i in {1..3}; do
              if git push; then
                echo "✅ Push realizado com sucesso (tentativa $i)"
                break
              else
                echo "⚠️ Falha no push (tentativa $i)"
                sleep 5
                if [ $i -eq 3 ]; then
                  echo "❌ Push falhou após 3 tentativas"
                  exit 1
                fi
              fi
            done
          fi

      - name: Firebase opcional (sem erro se falhar)
        if: env.FIREBASE_PROJECT_ID != ''
        run: |
          echo "🔥 Tentando Firebase (opcional)..."
          
          if [ -n "${{ secrets.FIREBASE_PROJECT_ID }}" ]; then
            echo "Firebase Project ID detectado"
            
            if [ -f "notification_sender.py" ]; then
              echo "📱 Executando notificações..."
              timeout 300 python notification_sender.py || echo "⚠️ Notificações falharam (não crítico)"
            fi
          else
            echo "ℹ️ Firebase não configurado (normal)"
          fi
        env:
          FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          FIREBASE_VAPID_KEY: ${{ secrets.FIREBASE_VAPID_KEY }}
        continue-on-error: true

  # Deploy no GitHub Pages
  deploy:
    needs: scrape
    runs-on: ubuntu-latest
    timeout-minutes: 10  # Timeout de 10 minutos para deploy
    if: needs.scrape.result == 'success'
    
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
      
    steps:
      - name: Deploy para GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        
  # Job de resumo final
  summary:
    needs: [scrape, deploy]
    runs-on: ubuntu-latest
    timeout-minutes: 5  # Timeout de 5 minutos para summary
    if: always()
    
    steps:
      - name: Resumo final da execução
        run: |
          echo ""
          echo "🎉 RESUMO DA EXECUÇÃO LIVELO ANALYTICS"
          echo "====================================="
          echo "⏰ Concluído: $(date +'%d/%m/%Y %H:%M:%S')"
          echo ""
          echo "📊 STATUS:"
          echo "Pipeline: ${{ needs.scrape.result == 'success' && '✅ Sucesso' || '❌ FALHOU - Email de notificação enviado' }}"
          echo "Deploy: ${{ needs.deploy.result == 'success' && '✅ Realizado' || '❌ Não realizado devido a falha anterior' }}"
          echo ""
          
          if [ "${{ needs.scrape.result }}" = "success" ]; then
            echo "🌐 ACESSO:"
            echo "📱 GitHub Pages: https://gcaressato.github.io/livelo_scraper/"
            echo ""
            echo "📁 ESTRUTURA:"
            echo "✅ index.html (página principal)"
            echo "✅ livelo_parceiros.xlsx (dados validados)"
            echo "✅ relatorios/livelo_analytics.html (relatório)"
            echo ""
            echo "🔍 VALIDAÇÃO RIGOROSA:"
            echo "✅ Mínimo 50 parceiros coletados"
            echo "✅ Arquivos com tamanho adequado"
            echo "✅ Conteúdo HTML bem formado"
            echo "✅ Dados Excel estruturados"
            echo ""
            echo "✅ Deploy realizado com sucesso!"
            echo "::notice::Sistema Livelo Analytics online - Dados validados com sucesso"
          else
            echo "❌ FALHA DETECTADA NO SCRAPING!"
            echo "🚨 Possíveis causas:"
            echo "   • Poucos dados coletados (< 50 parceiros)"
            echo "   • Mudanças na estrutura do site"
            echo "   • Problemas de conectividade"
            echo "   • Bloqueio por anti-bot"
            echo "   • Erro no script de scraping"
            echo "   • Arquivos com tamanho insuficiente"
            echo ""
            echo "::error::Scraping falhou - Verificar logs detalhados para causa específica"
          fi
